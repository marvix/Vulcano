<p class="text-center">
    @if(env('FACEBOOK_ENABLE'))
    <a href="#" class="btn btn-facebook btn-flat"><i class="fa fa-facebook"></i></i></a>
    @endif

    @if(env('TWITTER_ENABLE'))
    <a href="#" class="btn btn-twitter btn-flat"><i class="fa fa-twitter"></i></i></a>
    @endif

    @if(env('LINKEDIN_ENABLE'))
    <a href="#" class="btn btn-linkedin btn-flat"><i class="fa fa-linkedin"></i></i></a>
    @endif

    @if(env('GOOGLE_ENABLE'))
    <a href="#" class="btn btn-google btn-flat"><i class="fa fa-google"></i></i></a>
    @endif

</p>
<p class="text-center text-blue">- ou -</p>
