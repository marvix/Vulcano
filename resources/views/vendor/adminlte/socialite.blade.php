<p class="text-center">
    @if(env('FACEBOOK_ENABLE'))
    <a href="#" class="btn btn-facebook btn-flat"><i class="fa fa-facebook"></i></a>
    @endif

    @if(env('TWITTER_ENABLE'))
    <a href="#" class="btn btn-twitter btn-flat"><i class="fa fa-twitter"></i></a>
    @endif

    @if(env('LINKEDIN_ENABLE'))
    <a href="#" class="btn btn-linkedin btn-flat"><i class="fa fa-linkedin"></i></a>
    @endif

    @if(env('GOOGLE_ENABLE'))
    <a href="#" class="btn btn-google btn-flat"><i class="fa fa-google"></i></a>
    @endif

</p>
<p class="text-center text-blue">- ou -</p>
